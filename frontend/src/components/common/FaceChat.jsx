import React from 'react';
import Modal from 'react-modal';

const FaceChat = ({isOpen, })=>{

    return(
        <div>
            <Modal>
                
            </Modal>
        </div>
    )
}

export default FaceChat